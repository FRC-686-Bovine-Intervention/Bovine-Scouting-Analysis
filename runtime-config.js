// Local/test launchers may replace this file before starting the app.
// Production intentionally contains no simulator URL or routing mode.
globalThis.__EVENT_SIMULATOR_CONFIG = Object.freeze({ mode: "production" });
